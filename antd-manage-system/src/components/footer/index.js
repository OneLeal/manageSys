import React from 'react';
const { Component } = React;

export default class FooterComponent extends Component {
    render() {
        return (
            <div>
                Footer Component
            </div>
        );
    }
}