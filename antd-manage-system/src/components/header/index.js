import React from 'react';
const { Component } = React;

export default class HeaderComponent extends Component {
    render() {
        return (
            <div>
                Header Component
            </div>
        );
    }
}